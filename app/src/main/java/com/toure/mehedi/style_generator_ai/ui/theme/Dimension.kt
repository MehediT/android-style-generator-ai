package com.toure.mehedi.style_generator_ai.ui.theme

import androidx.compose.ui.unit.dp

// ===== Spacing System (échelle 8px) =====
object Spacing {
    val xxs = 4.dp   // micro-spacing, padding icons
    val xs = 8.dp    // spacing serré
    val s = 12.dp    // spacing compact
    val m = 16.dp    // spacing standard
    val l = 24.dp    // spacing large, sections
    val xl = 32.dp   // spacing très large
    val xxl = 48.dp  // spacing hero, grandes zones
    val xxxl = 64.dp // padding écrans desktop
}

// ===== Border Radius =====
object BorderRadius {
    val small = 4.dp    // inputs, petits éléments
    val medium = 8.dp   // cartes, boutons standards
    val large = 12.dp   // modales, grandes cartes
    val xLarge = 16.dp  // images featured, hero cards
    val round = 50      // avatars, badges circulaires (en %)
}
